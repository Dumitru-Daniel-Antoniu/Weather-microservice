# Weather-microservice
Client-server application that extracts weather data through a public API and returns them in a clean format

To execute the client:
docker exec -it weather_backend python -m client.client
